# Text Tools

Pure deterministic operations over text supplied by the caller. No filesystem, network, subprocess, or code execution.

## Operations

- `text.find`: find a literal or regular-expression pattern.
- `text.stats`: count lines, words, and characters.
