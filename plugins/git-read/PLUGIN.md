# Git Read

Runs a fixed allowlist of read-only Git commands in the active workspace. It does not accept arbitrary shell commands and does not mutate the repository.

## Operations

- `git.status`
- `git.diff`
- `git.log`
